<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#41B5E0"
    android:padding="24dp">

    <!-- Title at Center Top -->
    <TextView
        android:id="@+id/profileTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Complete your Profile"
        android:textSize="24sp"
        android:textColor="#FFFFFF"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="16dp"
        android:layout_alignParentTop="true" />

    <!-- Label for Name Field -->
    <TextView
        android:id="@+id/nameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Name(As per Aadhar)"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:layout_below="@id/profileTitle"
        android:layout_marginTop="32dp" />

    <!-- Name Field -->
    <EditText
        android:id="@+id/nameField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your name"
        android:layout_below="@id/nameLabel"
        android:layout_marginTop="4dp"
        android:padding="12dp"
        android:background="@drawable/rounded_edittext"
        android:textColor="#000000" />

    <!-- Label for Phone Number Field -->
    <TextView
        android:id="@+id/phoneLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Phone Number"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:layout_below="@id/nameField"
        android:layout_marginTop="16dp" />

    <!-- Phone Number Field -->
    <EditText
        android:id="@+id/phoneField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your phone number"
        android:layout_below="@id/phoneLabel"
        android:layout_marginTop="4dp"
        android:padding="12dp"
        android:background="@drawable/rounded_edittext"
        android:inputType="phone"
        android:textColor="#000000" />

    <!-- Gender Radio Group Label -->
    <TextView
        android:id="@+id/genderLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gender"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:layout_below="@id/phoneField"
        android:layout_marginTop="16dp" />

    <!-- Gender Radio Group -->
    <RadioGroup
        android:id="@+id/genderGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/genderLabel"
        android:layout_marginTop="4dp"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/radioMale"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Male"
            android:textColor="#FFFFFF" />

        <RadioButton
            android:id="@+id/radioFemale"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Female"
            android:textColor="#FFFFFF"
            android:layout_marginStart="16dp" />

        <RadioButton
            android:id="@+id/radioOther"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Other"
            android:textColor="#FFFFFF"
            android:layout_marginStart="16dp" />
    </RadioGroup>

    <!-- Label for Aadhar Number Field -->
    <TextView
        android:id="@+id/aadharLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Aadhar Number"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:layout_below="@id/genderGroup"
        android:layout_marginTop="16dp" />

    <!-- Aadhar Number Field -->
    <EditText
        android:id="@+id/aadharField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your Aadhar number"
        android:layout_below="@id/aadharLabel"
        android:layout_marginTop="4dp"
        android:padding="12dp"
        android:background="@drawable/rounded_edittext"
        android:inputType="number"
        android:textColor="#000000" />

    <!-- Label for Driving License Field -->
    <TextView
        android:id="@+id/drivingLicenseLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Driving License Number"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:layout_below="@id/aadharField"
        android:layout_marginTop="16dp" />

    <!-- Driving License Field -->
    <EditText
        android:id="@+id/drivingLicenseField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your license number"
        android:layout_below="@id/drivingLicenseLabel"
        android:layout_marginTop="4dp"
        android:padding="12dp"
        android:background="@drawable/rounded_edittext"
        android:inputType="text"
        android:textColor="#000000" />

    <!-- Buttons: Clear and Verify -->
    <LinearLayout
        android:id="@+id/linearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/drivingLicenseField"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Buttons: Clear and Verify -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <Button
                android:id="@+id/clearButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:backgroundTint="#FF0000"
                android:onClick="onClearClicked"
                android:text="Clear"
                android:textColor="#FFFFFF" />

            <Button
                android:id="@+id/verifyButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:backgroundTint="#080341"
                android:onClick="onVerifyClicked"
                android:text="Verify"
                android:textColor="#FFFFFF" />
        </LinearLayout>

        <!-- Help Label for Driving License Format -->
        <TextView
            android:id="@+id/helpLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Enter Driving License Number as: DL06/2024/4352"
            android:textColor="#FD0000"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- ProgressBar (centered) -->
    <ProgressBar
        android:id="@+id/verificationProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

    <!-- Setup E-Pay Button (shown only after successful submission) -->
    <Button
        android:id="@+id/setupPImageButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/helpLabel"
        android:layout_alignBottom="@id/linearLayout"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="69dp"
        android:layout_marginBottom="-53dp"
        android:backgroundTint="#4CAF50"
        android:onClick="onSetupPImageClicked"
        android:text="Set Up Your Profile Image"
        android:textColor="#FFFFFF"
        android:visibility="gone"
        tools:ignore="NotSibling" />


</RelativeLayout>
